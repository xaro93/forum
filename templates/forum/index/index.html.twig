{% extends 'forum/base.html.twig' %}

{% block body %}
    <ul>
        {% for thread in threads %}
            <li><a href="{{ path('forum_thread_show', {'slug': thread.slug}) }}">{{ thread }}</a></li>
        {% endfor %}
    </ul>

{% endblock %}